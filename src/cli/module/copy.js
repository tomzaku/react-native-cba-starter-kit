const chalk = require('chalk');
const log = console.log;


const copy = async (nameModule, pathSource, pathDist) => {
  // TODO: Copy module folder
  // Register module in dist App
  // Register reselect in dist App
  // Register action in dist App
  log(chalk.red('This function is not developer yet!, May be will update in next week'))
}
module.exports = copy